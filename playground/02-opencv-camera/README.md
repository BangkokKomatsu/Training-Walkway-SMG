# 02 - OpenCV Camera

พื้นที่ทดลอง: เปิด webcam/RTSP, อ่าน frame, จัดการกล้องหลุด/reconnect

รัน: `python playground/02-opencv-camera/example.py`
(ตั้งค่า `CAMERA_RTSP_URL` ใน `.env` เป็น `0` สำหรับ webcam หรือ path ไฟล์วิดีโอ)

> เนื้อหาเต็มอยู่ที่ `docs/course-modules/04-opencv-camera-basic.md` (Phase 5)
